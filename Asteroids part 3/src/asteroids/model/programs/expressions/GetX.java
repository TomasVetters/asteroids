package asteroids.model.programs.expressions;

public class GetX extends Method {
	public GetX(int line, int column, Expression e) {
		super(line,column,e);
	}
	
	@Override
	public Expression getValue() {
		double xCoor = 
		return new DoubleLiteral(this.getLine(), this.getColumn(), );
	}
}
